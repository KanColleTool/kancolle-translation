import missing
import crcpatch
import blacklist
import merge
import compile_

commands = [
	('missing', missing),
	('crcpatch', crcpatch),
	('blacklist', blacklist),
	('merge', merge),
	('compile', compile_),
]
